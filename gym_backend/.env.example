# JWT Configuration
# IMPORTANT: Generate a secure random secret key for production
# You can generate one using: openssl rand -hex 32
JWT_SECRET=your-secret-key-change-in-production-use-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Configuration
# SQLite (default)
DATABASE_URL=sqlite:///./gym_management.db
# For PostgreSQL, use format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://gymuser:gympass@localhost:5432/gym_db

# Initial Admin User (optional)
# If set, an admin user will be created on first startup
ADMIN_EMAIL=admin@gym.com
ADMIN_PASSWORD=changeme123

# Frontend Configuration
FRONTEND_ORIGIN=http://localhost:3000
